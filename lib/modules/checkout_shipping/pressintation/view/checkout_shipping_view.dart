import 'package:flutter/material.dart';

class CheckoutShippingView extends StatelessWidget {
  const CheckoutShippingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
